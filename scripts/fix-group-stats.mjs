import fs from 'node:fs';
const p='src/main.ts';
let s=fs.readFileSync(p,'utf8');
const battleBlock=/function showOnlineGroupStatBattle\(room:Room,rows:any\[\],matchIndex:number,statIndex:number,meWins:number,foeWins:number\)\{[\s\S]*?\}function unify/;
const replacement=`const groupBattleStats=[{label:'武力',index:0},{label:'知力',index:1}];
function showOnlineGroupStatBattle(room:Room,rows:any[],matchIndex:number,statIndex:number,meWins:number,foeWins:number){const statInfo=groupBattleStats[statIndex];if(!statInfo)return finishOnlineGroupBattle(room,rows,meWins,foeWins);const row=rows[matchIndex],me=row.meChar.stats[statInfo.index],foe=row.foeChar.stats[statInfo.index],result=me===foe?'引き分け':me>foe?'あなたの勝ち！':'相手の勝ち！',nextNo=matchIndex*2+statIndex+1;const nextLabel=statIndex===1?(matchIndex===rows.length-1?'決着を見る':'次の武将へ'):'知力戦へ';app.innerHTML=shell('<div class="content center"><div class="badge">三軍決戦 / '+row.role+'戦</div><div class="duel"><div><small>'+row.meName+' / '+row.role+'</small><h2>'+row.meChar.name+'</h2></div><div class="vs">VS</div><div><small>'+row.foeName+' / '+row.role+'</small><h2>'+row.foeChar.name+'</h2></div></div><div class="rounds"><div class="round-current"><span>'+statInfo.label+'</span><strong>'+me+' − '+foe+'</strong></div></div><div class="battle-result"><strong>'+result+'</strong><small>武力・知力を1項目ずつ戦います。</small></div>'+btn('nextStat',nextLabel)+'</div>');document.querySelector('#nextStat')!.addEventListener('click',async()=>{const b=document.querySelector<HTMLButtonElement>('#nextStat')!;b.disabled=true;b.textContent='相手を待っています…';const advance=()=>advanceOnlineGroupBattle(room,rows,matchIndex,statIndex,meWins+(me>foe?1:0),foeWins+(foe>me?1:0));try{const r=await api.post('/api/rooms/group-sync-next',{code:room.code,playerId,battleNo:nextNo});if(r.data?.ready){advance();return;}const timer=window.setInterval(async()=>{try{const q=await api.get('/api/rooms/group-sync-next?code='+encodeURIComponent(room.code)+'&battleNo='+nextNo);if(q.ready){clearInterval(timer);advance();}}catch{}},300);setTimeout(()=>clearInterval(timer),90000)}catch{b.disabled=false;b.textContent=nextLabel;alert('相手との同期に失敗しました。')}})}
function advanceOnlineGroupBattle(room:Room,rows:any[],matchIndex:number,statIndex:number,meWins:number,foeWins:number){if(statIndex<1)return showOnlineGroupStatBattle(room,rows,matchIndex,statIndex+1,meWins,foeWins);const duelResult=meWins===foeWins?'引き分け':meWins>foeWins?'あなたの勝ち！':'相手の勝ち！';if(matchIndex<rows.length-1){app.innerHTML=shell('<div class="content center"><div class="badge">'+rows[matchIndex].role+'戦 決着</div><h2>'+duelResult+'</h2><div class="power">'+meWins+' − '+foeWins+'</div>'+btn('nextDuel','次の武将へ')+'</div>');document.querySelector('#nextDuel')!.addEventListener('click',async()=>{const b=document.querySelector<HTMLButtonElement>('#nextDuel')!;b.disabled=true;b.textContent='相手を待っています…';const battleNo=(matchIndex+1)*2;const advance=()=>showOnlineGroupStatBattle(room,rows,matchIndex+1,0,0,0);try{const r=await api.post('/api/rooms/group-sync-next',{code:room.code,playerId,battleNo});if(r.data?.ready){advance();return;}const timer=window.setInterval(async()=>{try{const q=await api.get('/api/rooms/group-sync-next?code='+encodeURIComponent(room.code)+'&battleNo='+battleNo);if(q.ready){clearInterval(timer);advance();}}catch{}},300);setTimeout(()=>clearInterval(timer),90000)}catch{b.disabled=false;b.textContent='次の武将へ'}});return}finishOnlineGroupBattle(room,rows,meWins,foeWins)}
function finishOnlineGroupBattle(room:Room,rows:any[],meWins:number,foeWins:number){app.innerHTML=shell('<div class="content center"><div class="badge">三軍決戦 / FINAL</div><h2>三軍決戦、決着！</h2><p>先鋒 → 副将 → 大将の順で、各戦を<strong>武力・知力</strong>の2項目に分けて判定しました。</p><div class="power">'+meWins+' − '+foeWins+'</div>'+btn('home','タイトルへ',true)+'</div>');document.querySelector('#home')!.addEventListener('click',renderStart)}
function groupOnlineBattle(room:Room){const g=room.group,order=['先鋒','副将','大将'],nameFor=(roles:any,role:string)=>Object.entries(roles||{}).find(([,v])=>v===role)?.[0]||'—',meKey=room.host.id===playerId?'host':'guest',foeKey=meKey==='host'?'guest':'host',meName=meKey==='host'?room.host.name:room.guest.name,foeName=foeKey==='host'?room.host.name:room.guest.name,rows=order.map(role=>{const mn=nameFor(g.roles[meKey],role),fn=nameFor(g.roles[foeKey],role);return {role,meName,foeName,meChar:chars.find(c=>c.name===mn)||chars[0],foeChar:chars.find(c=>c.name===fn)||chars[0]};});const meList=rows.map(r=>'<strong>'+r.role+'：'+r.meChar.name+'</strong>').join(''),foeList=rows.map(r=>'<strong>'+r.role+'：'+r.foeChar.name+'</strong>').join('');app.innerHTML=shell('<div class="content center"><div class="badge">三軍決戦 / 対戦開始</div><div class="match-card"><div class="match-player"><span>'+meName+'</span>'+meList+'</div><div class="match-vs">VS</div><div class="match-player enemy"><span>'+foeName+'</span>'+foeList+'</div></div>'+btn('battleStart','先鋒戦を開始')+'</div>');document.querySelector('#battleStart')!.addEventListener('click',async()=>{const b=document.querySelector<HTMLButtonElement>('#battleStart')!;b.disabled=true;b.textContent='相手を待っています…';try{const r=await api.post('/api/rooms/group-sync-next',{code:room.code,playerId,battleNo:1});if(r.data?.ready){showOnlineGroupStatBattle(room,rows,0,0,0,0);return;}const timer=window.setInterval(async()=>{try{const q=await api.get('/api/rooms/group-sync-next?code='+encodeURIComponent(room.code)+'&battleNo=1');if(q.ready){clearInterval(timer);showOnlineGroupStatBattle(room,rows,0,0,0,0);}}catch{}},300);setTimeout(()=>clearInterval(timer),90000)}catch{b.disabled=false;b.textContent='先鋒戦を開始'}})}function unify`;
if(!battleBlock.test(s))throw new Error('group battle patch target not found');
s=s.replace(battleBlock,replacement);
fs.writeFileSync(p,s);
console.log('group stat battle patch applied');